build-variables:
  global:
    - ENABLE_COVERAGE=0
    - SENTRY_DSN_UI=0
  services:
    ui:
      - SERVER_HOST=http://api.ly.fish.local
      - SERVER_PORT=80
    livechat:
      - SERVER_HOST=http://api.ly.fish.local
      - SERVER_PORT=80
