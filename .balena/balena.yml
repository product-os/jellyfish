build-variables:
  global:
    - SENTRY_DSN_UI=0
  services:
    ui:
      - SERVER_HOST=https://api.ly.fish.local
      - SERVER_PORT=443
    livechat:
      - SERVER_HOST=https://api.ly.fish.local
      - SERVER_PORT=443
