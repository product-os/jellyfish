kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: jellyfish-ui
  namespace: {{{KATAPULT_KUBE_NAMESPACE}}}
  annotations:
    kubernetes.io/tls-acme: 'true'
spec:
  tls:
    - secretName: jellyfish-ui-tls
      hosts:
        - {{{JELLYFISH_HOST}}}
  rules:
    - host: {{{JELLYFISH_HOST}}}
      http:
        paths:
          - path: /
            backend:
              serviceName: jellyfish-ui
              servicePort: 80

