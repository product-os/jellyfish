apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: jellyfish-ui
  namespace: '{{{KATAPULT_KUBE_NAMESPACE}}}'
  name: jellyfish-ui
spec:
  selector:
    app.kubernetes.io/name: jellyfish-ui
  ports:
  - port: 80
  sessionAffinity: ClientIP
