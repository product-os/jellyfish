apiVersion: v1
kind: Service
metadata:
  labels:
    name: jellyfish
  namespace: jellyfish
  name: jellyfish
spec:
  selector:
    app: jellyfish
  ports:
  - port: 8000
    name: jellyfish-api
  sessionAffinity: ClientIP
