apiVersion: v1
kind: Service
metadata:
  labels:
    name: jellyfish-ui
  namespace: jellyfish
  name: jellyfish-ui
spec:
  selector:
    app: jellyfish-ui
  ports:
  - port: 80
    name: jellyfish-ui
