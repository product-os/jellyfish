{
  "description": "Jellyfish Card",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "type": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "tags": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true
    },
    "links": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true
    },
    "data": {
      "type": "object"
    }
  },
  "additionalProperties": false,
  "required": [ "id", "type", "tags", "links", "data" ]
}
